#!/bin/bash

{{- if eq .chezmoi.osRelease.id "ubuntu" }}
sudo apt update -y && apt install -y software-properties-common
sudo add-apt-repository -y --update ppa:ansible/ansible
sudo apt install -y ansible

ansible-playbook ubuntu.yml --tags init --ask-become-pass
{{- end }}
